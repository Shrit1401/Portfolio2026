/** Singleton document id for ordered build log references (see buildLogList schema). */
export const BUILD_LOG_LIST_ID = "siteBuildLogList";

/** Singleton for homepage rope / polaroid strip (see ropePolaroidGallery schema). */
export const ROPE_POLAROID_GALLERY_ID = "ropePolaroidGallery";
